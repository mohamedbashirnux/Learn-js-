Undefined

Undefined means "this variable exists, but nobody gave it a value yet." JS sets this automatically — you didn't choose it (that's the difference from null).

let city;
console.log(city); // undefined

👉 Basically: Undefined = "not given a value yet" (JS does this automatically).