# BigInt

BigInt is for numbers too huge for a regular Number to handle accurately. You add `n` at the end to make one.

```js
let big = 123456789012345678901234567890n;
```

👉 Basically: BigInt = for really really big numbers, add "n" at the end.