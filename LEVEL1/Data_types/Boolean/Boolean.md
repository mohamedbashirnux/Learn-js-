# Boolean

A Boolean is only ever `true` or `false`. Used for yes/no, on/off type answers.

```js
let isStudent = true;
let isRaining = false;
let age = 20;
console.log(age > 18); // true — comparisons return booleans too
```

👉 Basically: Boolean = true or false, nothing else.