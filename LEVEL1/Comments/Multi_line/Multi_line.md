# Multi-line Comment

Starts with `/*` and ends with `*/`. Everything in between is ignored, even across many lines. Good for longer explanations or blocking out a chunk of code temporarily.

```js
/*
This is a comment
that spans multiple
lines.
*/
```

👉 Basically: `/* ... */` = ignore everything in between, no matter how many lines.