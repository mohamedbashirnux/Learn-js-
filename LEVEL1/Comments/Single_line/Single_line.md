# Single Line Comment

Starts with `//`. Everything after it on that line is ignored by JS — used to explain code or leave yourself notes.

```js
// this whole line is a comment
let name = "Amiin"; // this part after // is also a comment
```

👉 Basically: `//` = ignore everything after this, on this line only.