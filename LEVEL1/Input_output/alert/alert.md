# alert()

Shows a popup message box in the browser. User just clicks "OK" to close it — no typing, no answer returned (unlike prompt).

```js
alert("Welcome to my site!");
```

⚠️ **Browser only.** Won't run with `node file.js`. Needs an HTML file opened in a browser to test.

👉 Basically: `alert()` = pop up a message on screen.