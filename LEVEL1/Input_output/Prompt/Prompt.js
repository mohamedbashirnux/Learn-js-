// prompt() example
// ⚠️ Browser only — will NOT run with "node prompt.js"
// To test this, put it inside an HTML file and open it in Chrome/Edge.

let name = prompt("What is your name?"); // shows a popup box, user types an answer
console.log(name); // prints whatever the user typed