// Logical operators

let age = 20;
let hasID = true;

console.log(age >= 18 && hasID); // true  - AND: both must be true
console.log(age >= 18 || hasID); // true  - OR: at least one must be true
console.log(!hasID);             // false - NOT: flips true to false (or false to true)